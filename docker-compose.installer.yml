services:
  installer:
    container_name: azuracast_installer
    image: 'ghcr.io/gasguti/bitstreaming-radio-panel:${AZURACAST_VERSION:-latest}'
    volumes:
      - './:/installer'
    restart: 'no'
    entrypoint: docker_installer
    command: install
    privileged: true
